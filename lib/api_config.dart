// api_config.dart — Ce fichier n'est plus utilisé.
// Le backend Firebase remplace toutes les URL HTTP.
// Conservé pour éviter des erreurs de compilation sur les imports existants.
class ApiConfig {
  static const String baseUrl = '';
}
